import { StyleSheet } from 'react-native';

export const buttonStyles = StyleSheet.create({
    button: {
      marginTop: 20,
      padding: 10,
      backgroundColor: '#ccc',
      borderRadius: 5,
    },
    buttonText: {
      fontSize: 16,
      textAlign: 'center',
    },
  });
  
  